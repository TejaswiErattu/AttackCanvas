/** The account the api uses to call the billing provider. */
// SEEDED:m-hardcoded-apikey
const apiKey = "seeded-demo-billing-key-not-a-secret";

export const serviceAccount = { id: "svc-billing", apiKey };
