# docs

Project documentation and the build log.

- [testing.md](testing.md): test count, coverage for detect, scoring, security and client,
  and what each test group proves.
- [../eval/bench/report.md](../eval/bench/report.md): the seeded bench (`pnpm bench`), with
  precision, recall and false-gap rate against the labelled apps in `tests/fixtures/seeded/`.
